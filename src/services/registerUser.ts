//contains services related to registering a user
const registerUser = {}

registerUser.registerWithEmail = (email : string)=> {
//taking an email and returns the created user
const data = {
    emailVerified : false,
    currentStep : 1,
    email : email
}
}








module.exports = registerUser